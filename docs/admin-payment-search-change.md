# Admin payment/search update

- Persist the selected on-site payment method (including PayPal) for regular bookings so the cashbook can display it correctly.
- Include historic/open/cancelled/rescheduling bookings in customer search.
- Keep real visit counts limited to past confirmed appointments.
- Simplify the admin hub by keeping the main quick actions visible and moving less-used areas into a collapsible section.
